# Задача: реализовать мобильное приложение Druna по Figma и REST API

Необходимо полностью реализовать мобильное приложение Druna для iOS и Android на основе существующего дизайна в Figma и контракта backend API.

Приложение должно визуально соответствовать дизайну, работать с реальным API, безопасно хранить авторизационные токены и корректно обрабатывать все основные состояния интерфейса.

## Исходные материалы

- Figma: `<ВСТАВИТЬ ССЫЛКУ НА FIGMA>`
- API-документация: `docs/FRONTEND_API.md`
- Backend-репозиторий: `https://github.com/TG4-Dev/DrunaServer`
- Base URL тестового backend: `<ВСТАВИТЬ URL>`
- Swagger: `<API_BASE_URL>/swagger/index.html`
- Тестовый пользователь:
  - username: `<ТЕСТОВЫЙ USERNAME>`
  - password: `<ТЕСТОВЫЙ PASSWORD>`
- Целевые платформы: iOS и Android
- Язык интерфейса: `<РУССКИЙ / АНГЛИЙСКИЙ / ОБА>`
- Минимальная версия iOS: `<ВЕРСИЯ ИЛИ ПРЕДЛОЖИ>`
- Минимальная версия Android: `<ВЕРСИЯ ИЛИ ПРЕДЛОЖИ>`

Если каких-либо данных нет, сначала попробуй получить их из репозитория, API-документации или Figma. Если вопрос не блокирует работу, прими разумное допущение, зафиксируй его и продолжай. Задавай вопрос только в том случае, если без ответа невозможно безопасно продолжить или решение существенно изменит продукт.

## Результат

На выходе должен получиться готовый проект мобильного приложения, который:

- собирается и запускается на iOS и Android;
- соответствует дизайну из Figma;
- подключён к DrunaServer;
- реализует основные пользовательские сценарии;
- корректно обрабатывает авторизацию и обновление токенов;
- содержит переиспользуемые UI-компоненты;
- имеет понятную структуру проекта;
- проходит проверку типов, линтер и тесты;
- содержит инструкции по установке, запуску и сборке.

Не ограничивайся аудитом или планом. После анализа самостоятельно переходи к реализации и продолжай работу до готового проверенного результата, если не обнаружен действительно блокирующий вопрос.

---

# 1. Изучение существующего проекта

Перед изменениями:

1. Изучи весь текущий репозиторий.
2. Найди и прочитай:
   - `README.md`;
   - `AGENTS.md` и все вложенные `AGENTS.md`;
   - `package.json`;
   - конфигурацию TypeScript;
   - конфигурацию линтера и форматтера;
   - существующую навигацию;
   - существующий API-клиент;
   - систему хранения состояния;
   - существующие UI-компоненты;
   - тесты;
   - конфигурацию сборки iOS и Android.
3. Не удаляй и не перезаписывай пользовательские изменения, не относящиеся к задаче.
4. Если проект уже инициализирован, продолжай использовать его архитектуру и зависимости, если они не создают существенных проблем.
5. Не проводи крупную миграцию технологий без необходимости.
6. Если проекта мобильного приложения ещё нет, создай его.

---

# 2. Выбор технологии

Предпочтительный стек:

- React Native;
- TypeScript в строгом режиме;
- функциональные компоненты;
- современная навигация;
- типизированный API-клиент;
- безопасное хранилище токенов;
- автоматические проверки качества.

Перед началом оцени, подходит ли Expo для требований приложения.

Используй Expo, если он покрывает:

- push-уведомления;
- secure storage;
- deep links;
- необходимые разрешения;
- выбранные библиотеки;
- сборки iOS и Android.

Если приложение требует возможностей, для которых bare React Native существенно предпочтительнее, используй bare React Native и кратко зафиксируй причину.

Не выбирай технологии только потому, что они популярны. Учитывай существующий проект, Figma, API, push-уведомления и требования к сборке.

---

# 3. Анализ Figma

Полностью изучи доступный Figma-файл.

Определи:

- все страницы и фреймы;
- экраны приложения;
- модальные окна;
- bottom sheets;
- вкладки;
- навигационные переходы;
- повторяющиеся компоненты;
- варианты компонентов;
- состояния кнопок и полей;
- типографику;
- цвета;
- сетку и отступы;
- радиусы;
- тени;
- иконки;
- изображения;
- иллюстрации;
- safe areas;
- размеры для разных устройств;
- светлую и тёмную темы, если они предусмотрены;
- состояния loading, empty, error, success, disabled и offline.

Не ориентируйся только на скриншоты. Используй реальные свойства элементов Figma: размеры, auto layout, spacing, стили, variables, component variants и экспортируемые assets.

Не используй изображения целых экранов вместо настоящей вёрстки.

Экспортируй только необходимые графические assets. Не дублируй средствами изображений то, что можно корректно реализовать обычными компонентами, текстом, стилями или SVG.

Если в дизайне отсутствуют обязательные состояния, спроектируй их в стиле существующей дизайн-системы и перечисли принятые решения.

---

# 4. Карта экранов и сценариев

До основной реализации составь рабочую карту:

| Экран | Путь открытия | API | Основные действия | Состояния |
|---|---|---|---|---|

Включи в неё как минимум:

- splash / восстановление сессии;
- onboarding, если есть в Figma;
- регистрация;
- вход;
- выход;
- главный экран;
- профиль;
- редактирование профиля;
- список событий;
- детали события;
- создание события;
- редактирование события;
- удаление события;
- личное свободное время;
- друзья;
- поиск или добавление друзей, если поддерживается API;
- группы;
- детали группы;
- события группы;
- уведомления и push-настройки, если они предусмотрены;
- системные ошибки;
- отсутствие сети;
- истёкшая сессия.

Сопоставь каждое действие с API endpoint из `docs/FRONTEND_API.md`.

Если дизайн показывает функцию, которой нет в API, не создавай фиктивную рабочую интеграцию. Реализуй UI только в том случае, если это полезно, явно отметь ограничение и не имитируй успешное сохранение данных.

Если API содержит функцию, которой нет в Figma, не добавляй новый продуктовый сценарий без необходимости. Зафиксируй расхождение.

---

# 5. Архитектура приложения

Организуй проект по функциональным областям, а не складывай весь код в общие папки.

Предпочтительная структура может включать:

- `app` — запуск приложения и общая композиция;
- `navigation` — навигация и типы маршрутов;
- `features/auth`;
- `features/profile`;
- `features/events`;
- `features/friends`;
- `features/groups`;
- `features/notifications`;
- `shared/api`;
- `shared/ui`;
- `shared/theme`;
- `shared/storage`;
- `shared/lib`;
- `shared/types`;
- `assets`.

Адаптируй структуру под существующий проект.

Разделяй:

- отображение;
- бизнес-логику;
- API-запросы;
- серверное состояние;
- локальное состояние;
- навигацию;
- безопасное хранение;
- преобразование DTO в модели интерфейса.

Не создавай абстракции заранее без реального повторного использования. Избегай как одного огромного глобального store, так и излишне раздробленной архитектуры.

---

# 6. Дизайн-система

На основе Figma создай единый слой дизайн-токенов:

- цвета;
- размеры;
- spacing;
- typography;
- border radius;
- shadows;
- icon sizes;
- animation durations, если нужны.

Создай переиспользуемые компоненты, когда они действительно повторяются:

- Button;
- IconButton;
- TextField;
- PasswordField;
- SearchField;
- Checkbox / Switch;
- Avatar;
- Card;
- ListItem;
- ScreenContainer;
- Header;
- TabBar;
- Modal;
- BottomSheet;
- EmptyState;
- ErrorState;
- LoadingState;
- Snackbar / Toast;
- подтверждение удаления.

Компоненты должны поддерживать состояния:

- default;
- pressed;
- focused;
- disabled;
- loading;
- error;
- selected, если применимо.

Не размножай значения цветов и размеров по файлам. Используй токены темы.

---

# 7. Навигация

Реализуй типобезопасную навигацию.

Предусмотри отдельные ветки:

1. Инициализация приложения.
2. Неавторизованный пользователь.
3. Авторизованный пользователь.

При запуске:

1. Покажи splash или предусмотренное дизайном состояние.
2. Загрузи сохранённые токены из secure storage.
3. Проверь возможность восстановить сессию.
4. Если access token действителен — открой приложение.
5. Если access token истёк, попробуй обновить пару токенов.
6. Если обновление невозможно — очисти сессию и открой вход.

Не показывай на мгновение экран входа авторизованному пользователю во время восстановления сессии.

Обработай Android back button, safe areas, открытие модальных экранов и повторное нажатие на вкладки.

---

# 8. Интеграция с API

Используй `docs/FRONTEND_API.md` как основной контракт.

API возвращает envelope:

```json
{
  "data": {},
  "error": null
}
```

или:

```json
{
  "data": null,
  "error": {
    "message": "Сообщение",
    "code": 400
  }
}
```

Реализуй централизованный типизированный API-клиент, который:

- использует настраиваемый base URL;
- добавляет `Content-Type: application/json`;
- добавляет `Authorization: Bearer <accessToken>` для защищённых маршрутов;
- при необходимости создаёт `X-Request-ID`;
- корректно разбирает response envelope;
- преобразует серверные ошибки в единый тип ошибки приложения;
- обрабатывает timeout;
- обрабатывает отсутствие сети;
- не логирует пароли и токены;
- не показывает пользователю технические stack traces;
- поддерживает отмену запросов, где это необходимо.

Не распределяй логику авторизации по отдельным экранам.

---

# 9. Авторизация и токены

Реализуй:

- регистрацию;
- вход;
- выход;
- восстановление сессии;
- обновление токенов;
- очистку недействительной сессии.

После регистрации выполни вход, если это соответствует пользовательскому сценарию.

Храни `accessToken` и `refreshToken` только в безопасном хранилище:

- iOS Keychain;
- Android Keystore;
- либо библиотеке, использующей эти механизмы.

Не храни токены:

- в AsyncStorage;
- в обычных файлах;
- в исходном коде;
- в логах;
- в незашифрованном глобальном состоянии, если этого можно избежать.

При получении `401`:

1. Убедись, что запрос использовал access token.
2. Запусти обновление токенов.
3. Не допускай параллельного запуска нескольких refresh-запросов.
4. Остальные запросы, получившие `401`, должны дождаться одного общего обновления.
5. Сохрани новую пару access/refresh token.
6. Старый refresh token больше не используй.
7. Повтори первоначальный запрос один раз.
8. Не создавай бесконечный цикл повторов.
9. Если refresh не удался — очисти токены и переведи пользователя на экран входа.

При ручном выходе очисти токены, локальное пользовательское состояние и кэш приватных данных.

---

# 10. Формы и валидация

Реализуй формы:

- регистрации;
- входа;
- редактирования профиля;
- создания события;
- редактирования события;
- других предусмотренных Figma форм.

Требования:

- понятная клиентская валидация;
- поддержка серверных ошибок;
- корректная клавиатура для email, пароля, чисел и дат;
- управление фокусом;
- переход между полями с клавиатуры;
- скрытие клавиатуры;
- защита от повторной отправки;
- loading state кнопки;
- сохранение введённых данных при исправимой ошибке;
- доступные подписи полей;
- пароль минимум 8 символов, согласно API;
- сообщения об ошибках в стиле интерфейса.

Не заменяй серверную валидацию клиентской. Клиентская валидация нужна для удобства, серверная остаётся источником истины.

---

# 11. События

Реализуй доступные по API и Figma сценарии:

- получение списка событий;
- просмотр данных события;
- создание;
- редактирование;
- удаление;
- обновление списка после изменений;
- личное свободное время;
- события групп.

Предусмотри:

- loading;
- pull-to-refresh, если уместно;
- пустой список;
- ошибку загрузки и повтор;
- optimistic update только там, где его можно безопасно откатить;
- подтверждение удаления;
- корректную обработку дат и часовых поясов;
- единый формат дат;
- предотвращение двойного создания при повторном нажатии.

Не предполагай формат даты — используй формат из API-контракта.

---

# 12. Профиль, друзья и группы

Реализуй поддерживаемые дизайном и API операции:

## Профиль

- загрузка текущего пользователя;
- отображение данных;
- редактирование;
- обновление интерфейса после сохранения;
- ошибки и повторная отправка.

## Друзья

- список друзей;
- доступные операции добавления, поиска, принятия или удаления — только если они есть в API;
- пустое состояние;
- ошибки и обновление данных.

## Группы

- список групп;
- просмотр группы;
- доступные действия с группой;
- события группы;
- пустые состояния;
- обновление данных после изменений.

Не показывай пользователю успешный результат, пока сервер не подтвердил операцию, если не реализован безопасный optimistic update.

---

# 13. Push-уведомления

Изучи раздел push-уведомлений в API-документации и требования Figma.

Если push входит в текущую версию:

- запроси разрешение в подходящий момент, а не сразу при первом запуске без объяснения;
- корректно обработай отказ;
- получи push token устройства;
- зарегистрируй его на backend, если endpoint доступен;
- обновляй token при его изменении;
- обработай уведомление в foreground;
- обработай переход по уведомлению;
- не ломай приложение, если push недоступен в симуляторе;
- добавь необходимые инструкции по настройке APNs/FCM;
- не добавляй реальные ключи и сертификаты в репозиторий.

Если backend или Figma не позволяют закончить интеграцию, подготовь инфраструктуру и точно опиши недостающие данные.

---

# 14. Конфигурация окружений

Поддержи минимум:

- development;
- production.

При наличии staging добавь его отдельно.

Вынеси из исходного кода:

- API base URL;
- идентификаторы окружения;
- настройки push;
- другие изменяемые параметры.

Создай безопасный пример конфигурации, например `.env.example`, без секретов.

Убедись, что:

- локальный iOS Simulator может обращаться к нужному адресу;
- Android Emulator использует `10.0.2.2`, если backend работает на машине разработчика;
- физическое устройство использует доступный LAN или HTTPS URL;
- production использует HTTPS;
- release-сборка не зависит от локального адреса.

---

# 15. Состояния интерфейса

Для каждого экрана предусмотрены:

- initial;
- loading;
- refreshing;
- success;
- empty;
- validation error;
- server error;
- unauthorized;
- offline;
- disabled;
- submitting.

Если состояние отсутствует в Figma, оформи его с использованием той же визуальной системы.

Не оставляй пустой белый экран при ошибке или отсутствии данных.

Сообщения пользователю должны быть понятными. Технические детали можно записывать только в безопасный development log без токенов, паролей и персональных данных.

---

# 16. Адаптивность

Интерфейс должен корректно работать:

- на компактных iPhone;
- на современных iPhone с Dynamic Island;
- на типичных Android-устройствах;
- с системными safe areas;
- с открытой клавиатурой;
- при длинных текстах;
- при увеличенном системном размере шрифта в разумных пределах;
- в портретной ориентации.

Не используй жёсткое позиционирование всего экрана по координатам из одного Figma-фрейма.

При конфликте между абсолютной пиксельной точностью и устойчивой адаптивной вёрсткой выбирай устойчивую вёрстку, сохраняя визуальную близость к Figma.

---

# 17. Доступность

Добавь базовую доступность:

- accessibility labels для кнопок без текста;
- достаточные зоны нажатия;
- понятный порядок фокуса;
- связь ошибок с полями;
- корректные роли кнопок, полей и заголовков;
- отсутствие критически важной информации, передаваемой только цветом;
- поддержку системного размера текста там, где это не разрушает интерфейс.

---

# 18. Производительность

Избегай:

- лишних повторных запросов;
- бесконечных циклов рендеринга;
- тяжёлых вычислений внутри render;
- загрузки огромных изображений;
- повторной регистрации обработчиков;
- неограниченного роста кэша;
- последовательных запросов, которые можно безопасно выполнить параллельно.

Оптимизируй только подтверждённые или очевидные проблемы. Не усложняй код микропроизводительными абстракциями без необходимости.

---

# 19. Тестирование

Добавь тесты как минимум для:

## API-клиента

- успешный response envelope;
- server error envelope;
- сетевую ошибку;
- timeout;
- авторизационный заголовок.

## Авторизации

- успешный вход;
- сохранение токенов;
- восстановление сессии;
- успешное обновление токенов;
- сохранение новой пары;
- одновременные `401` вызывают только один refresh;
- исходный запрос повторяется только один раз;
- неуспешный refresh очищает сессию;
- logout удаляет приватные данные.

## Бизнес-сценариев

- загрузка списка событий;
- пустой список;
- создание события;
- редактирование;
- удаление;
- ошибка сервера;
- основные формы и их валидация.

Если в проекте настроены end-to-end тесты, добавь smoke-сценарий:

1. Запуск приложения.
2. Вход.
3. Открытие профиля.
4. Открытие списка событий.
5. Создание тестового события.
6. Проверка появления события.
7. Удаление тестового события.
8. Выход.

Не делай тесты зависимыми от production.

---

# 20. Визуальная проверка

После реализации каждого экрана:

1. Запусти приложение.
2. Открой экран в целевом размере.
3. Сравни его с соответствующим фреймом Figma.
4. Проверь:
   - размеры;
   - отступы;
   - выравнивание;
   - шрифты;
   - цвета;
   - радиусы;
   - тени;
   - иконки;
   - изображения;
   - состояние клавиатуры;
   - safe areas;
   - прокрутку;
   - состояния загрузки и ошибок.
5. Исправь заметные расхождения.
6. Проверь экран минимум на одном размере iOS и одном размере Android.

Не считай экран завершённым только потому, что он функционально работает.

---

# 21. Качество кода

Требования:

- TypeScript strict mode;
- отсутствие необоснованного `any`;
- понятные названия;
- небольшие компоненты с ясной ответственностью;
- отсутствие дублирования бизнес-логики;
- единый формат ошибок;
- отсутствие секретов;
- отсутствие токенов и паролей в логах;
- отсутствие закомментированных крупных блоков устаревшего кода;
- отсутствие временных mock-данных в production-пути;
- отсутствие неиспользуемых зависимостей;
- отсутствие предупреждений React о keys и lifecycle;
- форматирование и линтер проходят.

Не подавляй ошибки TypeScript или линтера без обоснованной причины.

---

# 22. Документация проекта

Обнови или создай `README.md`.

Опиши:

- назначение приложения;
- технологический стек;
- требования к окружению;
- установку зависимостей;
- настройку `.env`;
- запуск iOS;
- запуск Android;
- подключение к локальному backend;
- запуск тестов;
- запуск линтера;
- проверку типов;
- создание development-сборки;
- создание release-сборки;
- настройку push-уведомлений;
- известные ограничения;
- структуру каталогов.

Добавь `.env.example` без реальных секретов.

Если для работы нужны ручные действия в Xcode, Android Studio, Apple Developer или Firebase, перечисли их пошагово.

---

# 23. Порядок реализации

Работай вертикальными функциональными срезами.

Рекомендуемый порядок:

## Этап 1. Аудит

- изучить репозиторий;
- изучить Figma;
- изучить API;
- составить карту экранов;
- зафиксировать расхождения;
- определить стек и архитектуру.

## Этап 2. Основа приложения

- конфигурация;
- тема;
- дизайн-токены;
- навигация;
- базовые UI-компоненты;
- API-клиент;
- типы ошибок;
- secure storage.

## Этап 3. Авторизация

- splash;
- регистрация;
- вход;
- восстановление сессии;
- refresh;
- logout;
- тесты авторизации.

## Этап 4. Профиль

- просмотр;
- редактирование;
- loading/error states;
- тесты.

## Этап 5. События

- список;
- детали;
- создание;
- редактирование;
- удаление;
- свободное время;
- тесты.

## Этап 6. Друзья и группы

- друзья;
- группы;
- детали группы;
- события группы;
- состояния и тесты.

## Этап 7. Уведомления

- разрешения;
- регистрация устройства;
- обработка уведомлений;
- deep links, если поддерживаются.

## Этап 8. Финальная проверка

- визуальное сравнение с Figma;
- iOS;
- Android;
- проверка типов;
- линтер;
- тесты;
- README;
- удаление временного кода.

После каждого этапа запускай доступные проверки и исправляй обнаруженные проблемы перед переходом дальше.

---

# 24. Работа с неизвестностями

Используй следующий приоритет источников:

1. Фактическое поведение backend и Swagger.
2. `docs/FRONTEND_API.md`.
3. Figma для визуального представления и пользовательского сценария.
4. Существующий код приложения.
5. Разумное явно зафиксированное допущение.

Если API-документация расходится с фактическим ответом тестового сервера, зафиксируй конкретное расхождение с endpoint, запросом и ответом.

Если Figma расходится с API:

- не скрывай проблему;
- не выдумывай endpoint;
- реализуй доступную часть;
- укажи, что требуется от дизайнера или backend-разработчика.

Не останавливай всю работу из-за локальной неоднозначности, если можно продолжать другие независимые части.

---

# 25. Безопасность

Запрещено:

- добавлять production-секреты в репозиторий;
- хранить токены в обычном локальном хранилище;
- логировать пароль или JWT;
- отключать проверку HTTPS в production;
- принимать refresh token как access token;
- бесконечно повторять запросы;
- использовать реальные пользовательские данные в тестах;
- обходить ограничения backend;
- скрывать ошибки небезопасными заглушками.

Проверь, что приватные данные одного пользователя не остаются в кэше после выхода и входа другого пользователя.

---

# 26. Критерии готовности

Задача считается завершённой, когда:

- приложение запускается на iOS;
- приложение запускается на Android;
- интерфейс соответствует Figma;
- регистрация и вход работают;
- сессия восстанавливается после перезапуска;
- refresh token корректно ротируется;
- logout полностью очищает сессию;
- профиль загружается и редактируется;
- события загружаются и изменяются;
- друзья и группы работают в пределах API;
- обязательные loading, empty, error и offline states реализованы;
- конфигурация окружений вынесена из кода;
- TypeScript-проверка проходит;
- линтер проходит;
- тесты проходят;
- в репозитории нет секретов;
- README позволяет другому разработчику запустить проект;
- известные ограничения перечислены явно.

---

# 27. Формат отчёта

В начале работы кратко сообщи:

- что найдено в репозитории;
- какой стек используется;
- какие экраны найдены в Figma;
- какие основные API-сценарии обнаружены;
- какие есть критические расхождения;
- в каком порядке будет выполняться реализация.

После этого приступай к реализации.

В процессе сообщай о завершении крупных этапов, но не останавливайся после каждого этапа в ожидании подтверждения, если подтверждение не требуется.

В финальном отчёте укажи:

1. Что реализовано.
2. Какие архитектурные решения приняты.
3. Какие проверки выполнены и их результат.
4. Как запустить приложение.
5. Какие переменные окружения нужны.
6. Какие ограничения или незавершённые интеграции остались.
7. Какие действия требуются от владельца проекта, если нужны внешние ключи, сертификаты или доступы.
8. Ссылки на основные изменённые файлы.

Главная цель — не просто создать набор экранов, а предоставить работающее, проверенное мобильное приложение, визуально соответствующее Figma и корректно интегрированное с DrunaServer.